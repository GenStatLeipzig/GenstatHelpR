#' @title Best Guess Genotype
#' @description This is a little test script
#' @param data a matrix with genotype data in impute2 format (triples with AA, AB or BB genotype chances)
#' @param threshold a cut off parameter for determining the best guess genotype according to genotype values for AA, AB and BB

calcBestGuessGenotype_kh = function(data, threshold) {
  ##
  print("This function still needs to be implemented.")
}
